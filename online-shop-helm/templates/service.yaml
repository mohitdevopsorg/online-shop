apiVersion: v1
kind: Service
metadata:
  name: {{ include "online-shop-helm.fullname" . }}
  labels:
    {{- include "online-shop-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "online-shop-helm.selectorLabels" . | nindent 4 }}
